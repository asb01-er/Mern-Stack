datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL") // put your MongoDB URI in .env
}

generator client {
  provider = "prisma-client-js"
}

model Book {
  id    String @id @map("_id") @default(auto()) @db.ObjectId
  title String
  link  String
  img   String
}